# Dependencies
# We don't have a node_modules folder, but this is standard practice.
node_modules/

# Environment variables
# This is the most important part. It prevents your .env file from being uploaded.
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Vercel
# Vercel may create a .vercel folder for local configuration.
.vercel

# OS generated files
# These files are created by your operating system and are not needed in the repo.
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# IDE/Editor files
# These are configuration files for code editors like VS Code.
.vscode/
.idea/
*.swp
*.swo
*~
